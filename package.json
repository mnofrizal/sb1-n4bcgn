{
  "name": "nodejs-express-api",
  "version": "1.0.0",
  "description": "A Node.js API using Express framework with best practice structure",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "jest",
    "setup": "npm install && npx prisma generate && npm run dev"
  },
  "dependencies": {
    "@prisma/client": "^4.14.0",
    "express": "^4.17.1",
    "dotenv": "^10.0.0",
    "cors": "^2.8.5",
    "helmet": "^4.6.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.12",
    "jest": "^27.0.6",
    "prisma": "^4.14.0"
  }
}